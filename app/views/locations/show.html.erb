<h1><%= @location.name %></h1>

<%= link_to 'Edit', edit_location_path %> | <%= link_to 'DELETE' , location_path(@location), method: :delete, data: {confirm: "Are you sure you want to delete '#{@location.name}'?"} %>

  <%= render 'single_location', location: @location %>
